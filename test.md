# 1 Введение
## 1 Какой оператор в Python используется для проверки соответствия условия (ветвление)?
### 1) for
### 2) while
### 3) if
### 4) def
# Ответ: 3

## 2 Какие из перечисленных способов позволяют вывести строку «Hello, World!» в Python?
### 1) print(Hello, World!)
### 2) print("Hello, World!")
### ) print('Hello, World!')
### 4) console.log("Hello, World!")
### 5) echo "Hello, World!"
### 6) printf("Hello, World!")
#Ответы: 2, 3


# 2 Hello, world!
## 1 Какой вызов print() выведет на экран слово goodbye?
### 1) 'goodbye'
### 2) print goodbye
### 3) print (‘goodbye’)
### 4) print (“goodbye’)
# Ответ: 3
	
## 2 Что важно для имени функции print в Python?
### 1) Регистр важен: print и Print - разные имена
### 2) Регистр не важен: print и Print работают одинаково
### 3) Важно только число букв, а не их регистр
### 4) Имя print зарезервировано, но его можно переопределить (присвоить)
### 5) Если написать Print, Python автоматически вызовет print без ошибки
###  6) print — это ключевое слово, как if или for
# Ответ: 2, 4


# 3 Комментарии
## 1 Как правильно временно отключить выполнение этой строки?
```
print('Do not print me')
```
###	1) // print('Do not print me')
###	2) # print('Do not print me')
###	3) /* print('Do not print me') */ 
###	4) ' print('Do not print me')
# Ответ: 2

## 2 Для чего в Python обычно используют комментарии?
###	1) Чтобы остановить пояснение для людей, которые читают код
###	2) Чтобы Python выполнял код быстрее
###	3) Чтобы заменить ими строки с рабочим кодом
###	4) Чтобы временно отключить часть кода при отладке
###	5) Чтобы объяснить, что делает сложный фрагмент кода 
###	6) Чтобы Python игнорировал определённые строки во время выполнения
# Ответ: 1,5


# 4 Инструкции (Statements)
## 1 Эквивалентен ли код в одну строку через ; к коду на двух строках?
```
print (‘A’); print (‘B’)
```
###	1) Нет, второй print всегда игнорируется
###	2) Да, выполнятся те же две инструкции в том же порядке
###	3) Нет, через ; в Python нельзя писать инструкции
###	4) Да, но только если между инструкциями поставить пробел
# Ответ: 2

## 2 Как обычно записывают несколько инструкций в учебных и рабочих программах?
###	1) Только в одну строку через двоеточие
###	2) Всегда в одну строку через запятую
###	3) Каждую инструкцию на новой строке
###	4) Каждую инструкцию на новой строке с нумерацией (1., 2., 3.)
###	5) Каждую инструкцию на новой строке с маркировкой (•, —, *)
###	6) В виде нумерованного списка с отступом и точкой с запятой в конце строки
# Ответ: 3, 4


# 5 Как мы проверяем ваши решения:
## 1 В сообщении теста есть фрагмент:
```
E   AssertionError: assert '9' == '10'
E
E     - 10
E     + 9
```
# Что здесь ожидает тест?
###	~AssertionError
###	~10 (+)
###	~9
###	~10.0000000000000003
# ОТВЕТ: 2

## 2 Почему тесты могут запускать код на разных входных данных в более сложных задачах?
###	~Чтобы случайно менять условие задачи
###	~Чтобы программа работала быстрее
###	~Чтобы проверить, что решение работает не только в одном частном случае (+)
###	~Чтобы упростить взаимодействие с Python
###	~Потому что используется дискретное построение кода
###	~Для проверки корректности и устойчивости во всех случаях (+)
# ОТВЕТ: 3,6


# 6 Синтаксические ошибки:
## 1 Что не так в коде?
# print('Hodor)
###	~Нельзя использовать print()
###	~Не закрыта строка: пропущена кавычка (+)
###	~В Python нельзя выводить слово Hodor
###	~Переменная “Hodor” не была создана
# ОТВЕТ: 2

## 2 Что стоит проверить первым делом после SyntaxError?
###	~Указанную строку и строку перед ней (+)
###	~Только последнюю строку файла
###	~Сразу переписать программу с нуля
###	~Первые 3 строки файла
###	~Проверить логику программы на вводе простейших данных (+)
###	~Проверить версию интерпретатора Python, заменить при несоответствии
# ОТВЕТ: 1,5


# 7 Арифметические операции
## 1 Какой оператор используется для возведения в степень в Python?
###	~** (+)
###	~%
###	~^
###	~*
# ОТВЕТ: 1

## 2 Чему равно выражение `14 % 4
###	~3
###	~0
###	~2 (+)
###	~3,5
###	~4
###	~√4 (+)
# ОТВЕТ: 3,6


# 8 Операторы
## 1 В выражении 8 + 2 что является операндами?
###	~Только число 10
###	~8 и 2 (+)
###	~+ и =
###	~= и 10
# ОТВЕТ: 2

## 2 Что такое оператор в программировании?
###	~Символ или слово, которое задает действие (+)
###	~Только число в выражении
###	~Комментарий в коде
###	~Переменная, которой присвоили значение
###	~Команда, обозначающая любое логическое действие над операндами (+)
###	~Символ, на который ссылается 
# ОТВЕТ: 1,5


# 9 Коммутативная операция
## 1 Что выведет программа?
```
print(3 + 2)
print(2 + 3)
```
###	1) Сначала 6, потом 5
###	2)  Сначала 5, потом 1
###	3)  Оба раза будет 5       +
###	4) Оба раза будет 6	    
# ОТВЕТ: 3

## 2 Является ли возведение в степень коммутативным для всех чисел?
###	1) Да, порядок всегда не важен
###	2) Да, но только для целых чисел
###	3) Нет, например 2 ** 3 не равно 3 ** 2   +
# ОТВЕТ: 3


# 10 Композиция операций

## 1 Что выведет код?
```
print(2 * 4 * 5 * 10)
```
###	200
###	400     +
###	40
# ОТВЕТ: 2

## 2 Что выведет код?
```
print(6 - -2)
```
###	1) 4
###	2) -8
###	3) 8          +
###	4) ничего из перечисленного 
# ОТВЕТ: 3


# 11 Приоритет
## 1 Какие варианты расстановки скобок в выражении 2 + 3 * 1 + 2 дадут результат 11?
###	1) print(((2 + 3) * 1) + 2)
###	2) print(2 + 3 * (1 + 2) + 2)
###	3) print((2 + 3) * (1 + 2))
###	4) print(2 + (3 * (1 + 2)))      +
###	5) print(2 + 3 * (1 + 2))         +
# ОТВЕТ: 4,5

## 2  Что будет выведено на экран?
```
print(2 + 3 * 2)
```
###	1)10
###	2) 8     +
###	3) 12
###	4) 18
# ОТВЕТ: 2


# 12 Числа с плавающей точкой
## 1 Какие выражения при умножении на 2 дадут результат 15.0?
###	1) print(7.5 * 2)     +
###	2) print(2 * 7.5)     +
###	3) print(7.0 * 2)
###	4) print(7 * 2)
###	5) print(15 / 2)
# ОТВЕТ: 1,2

## 2 Чему равно 0.2 + 0.1 в Python?
###	1) 0.30000000000000004      +
###	2) 0.3 ровно
###	3) не решается
###	4) равно нулю
# ОТВЕТ: 1


# 13 Линтер
## 1 Что такое линтер и зачем его исправлять?
###	1.программа для проверки кода на соответствие стандартам
кодирования, его нужно исправлять, чтобы код было проще читать и поддерживать
###	2.стандарт для написания и редактирования кода, его не нужно исправлять, ведь он не влияет на работу программы
###	3. программа для тестирования и исполнения кода, его нужно исправлять для оптимизации, ведь код без стилистических ошибок работает быстрее
###	4.программа для тестирования и исполнения кода, его нужно исправлять, чтобы код было проще читать и поддерживать
# ОТВЕТ: 1

## 2 Какой стандарт используется при написании кода на Python, поможет ли линтер найти некоторые синтаксические ошибки еще до запуска?
###	1.стандарт, используемый при написании кода flake8, линтер не поможет найти некоторые синтаксические ошибки
###	2. стандарт, используемый при написании кода flake8, линтер поможет найти некоторые синтаксические ошибки
###	3. стандарта, используемого при написании кода нет, программист сам выбирает стиль оформления, линтер может помочь найти некоторые синтаксические ошибки
###	4. стандарта, используемого при написании кода нет, программист сам выбирает стиль оформления, линтер не сможет помочь найти синтаксические ошибки
###	5. стандарт, который используется для написания кода PEP8, в котором линтер поможет найти синтаксические ошибки
###	6. стандарт, используемый при написании кода PEP8, линтер поможет найти некоторые синтаксические ошибки
# ОТВЕТ: 5,6

# 14 Строки 
## 1 Выберите верные примеры строк в Python:
###	1. “Who’s to blame?”
###	2. “computer”
###	3. “”
###	4. “Test “Drive” or Track”
###	5. “Dragon\”s Song”
###	6. “Hi, i`m”Peter”, what your name?”
# ОТВЕТ: 1,2,3

## 2 Есть ли отличия пустой строки ‘’ и строки с пробелом ‘ ‘?
###	1. Нет, если визуально они одинаковые, то значит они равны
###	2. Да, пустая строка не содержит символов, а строка с пробелом содержит пробел
###	3. Нет, потому что отличия зависят от языка программирования
###	4. Они отличаются только при выводе на экран, а в памяти и при сравнении считаются равными
# ОТВЕТ: 2


# 15 Экранированные последовательности
## 1 Как можно выводить строку, чтобы апостроф не портил код? Dragon's song
###	1. print("Dragon's\nsong")
###	2. print('Dragon\'s\nsong')
###	3. print("Dragon\"s/ song")
###	4. print('Dragon"s/n song')
###	5. print('Dragon's\ song')
###	6. print(“Dragon's/nsong')
# ОТВЕТ: 1,2

## 2 Для экранирования используется символ \. А как можно вывести сам обратный слэш и как сделать перевод строки?
###	1. Экранировать его таким же символом, использовать \n
###	2. обернуть его в двойные кавычки, это невозможно
###	3. обернуть его в одинарные кавычки, нажать на Enter
###	4. экранировать его противоположным символом, использовать \n
# ОТВЕТ: 1


# 16 Конкатенация
## 1 Какой оператор используется для объединения строк в Python и какая строка получится в результате конкатенации “big” + “ben”?
###	1. +, “bigben”
###	2. -, ”ben big”
###	3. *, ”big ben”
###	4. \, ”benbig”
# ОТВЕТ: 1

## 2 Добавляет ли оператор + пробел между строками автоматически, какие варианты выдадут Drive car?
###	1.Нет, print(‘Drive ’ + ’car’)
###	2.Да, print(‘Drive’+‘car’)
###	3.Нет, print(‘Drive’ + ‘ ’ + ‘car’)
###	4.Да, print(‘Drive’ + ‘’ + ‘car’)
###	5.Да, print(“Drive”+ “ ” +” car”)
###	6.Нет, print(‘Drivecar’)
# ОТВЕТ: 1,3


# 17 Кодировка

## 1 Какой самый распространенный формат кодировки?
###	1) ASCII
###	2) UTF-8		+ 
###	3) KOI-8
###	4) Windows-1251
###	5) MacCyrillic
###	6) CP866
# ОТВЕТ: 2

## 2 Что происходило с текстом, если использовалась другая кодировка?
###	1) Печатались не те символы и текст был не читаем	+
###	2) Выдавало ошибку о неправильной кодировке
###	3) Ничего не печаталось
###	4) Текст превращался в другой тип данных (Звуковой и т.д.)
# ОТВЕТ: 1

# 18 Что такое переменная?

## 1 Где правильно объявляется переменная?
###	1) x = 2					+
###	2) 1_x = 3
###	3) x => 2
###	4) x += 4
###	5) 6 = x					+
###	6) x = “Hello, World!”	+
# ОТВЕТ: 1,5,6

## 2 -
###	


# 19 -----
## 1

## 2
###	
# 20 ------
## 1

## 2

# 21  Выражения в определениях
## 1 Какое значение будет напечатано на экран?
```
first = 'brave'
middle = 'new'
last = 'world'
space = ' '
print(first + space + middle + last)
```
###	1) brave new world
###	2) bravenewworld
###	3) brave newworld      +
###	4) bravenew world
# ОТВЕТ: 3

## 2  Какое значение будет выведено на экран?
```
apples_per_box = 6
boxes_per_container = 5
container = 4
summa = 2
apples_count = apples_per_box * boxes_per_container * container
apples_count = container  *  summa * boxes_per_container 
print(apples_count)
```
###	1)  100
###	2) 200
###	3) 120      
###	4) 60
###	5) 130
###	6) 40        +
# ОТВЕТ: 6


# 22  Переменные и конкатенация
## 1 Какие варианты кода выведут строку database, если first = 'base', а second = 'data' ?
###	1) print(second * 2)
###	2) print(first + second)
###	3) print(second + first)       +
###	4) print(second + ' ' + first)
###	5) print('data' + 'base')      +
# ОТВЕТ: 3,5

## 2 Какой из вариантов кода выведет строку ‘database’?
### a = ‘data’
### b = ‘base’
### c = 2
### 1)  print (a + b)
### 2) print (b + a)
### 3) print (a + str(c) + b)       +
### 4) print ((a + b) * c)
### 5) print (b + str(c) + a)
# ОТВЕТ: 3


# 23 Именование переменных 
## 1 Какое имя лучше подойдёт для переменной, которая хранит количество непрочитанных уведомлений?
### 1) notifications_unread_count
### 2) unread_notifications_count        +
### 3) unreadNotificationsCount
### 4) unread-notifications-count
### 5) unread_notif_count
### 6) unread_count
# ОТВЕТ: 2

## 2 Какой стиль именования переменных используется в Python?
### 1) lowerCamelCase
### 2) CamelCase
### 3) snake_case      +
### 4) kebab-case
### 5) doner_case      +
# ОТВЕТ: 3,5


# 24 Магическая числа 
## 1 Как правильнее поступать с числовыми коэффициентами, вроде курса валют, чтобы код было легче читать и сопровождать?
### 1) Дублировать числа в каждом месте использования
### 2) Записывать числа прямо в выражениях
### 3) Вынести их в переменные с понятными именами        +
### 4) Спрятать числа в комментариях
# ОТВЕТ: 3

## 2 Что из перечисленного НЕ является магическим числом?
### 1) Число 86400, записанное прямо в формуле
### 2) Число 1.25, использованное без пояснений
### 3) Любое число, значение которого неясно из кода
### 4) Значение, сохраненное в переменной seconds_in_day       +
# ОТВЕТ: 4


# 25 Константы
## 1 Как по соглашению именуются константы в Python?
###	~ lowerCamelCase
###	~ UPPER_SNAKE_CASE (+)
###	~ MaX-UsErS
###	~ lower snake case
# ОТВЕТ: 2

## 2 Являются ли константы неизменяемыми на уровне языка?
###	~ Да, изменение запрещено синтаксисом
###	~ Нет, это соглашение об именовании (+)
###	~ Да, но после того как вписали константы
###	~ Hадо удалить и создать новый
###	~ Нет потому, что весь код сломается
###	~ Можно изменить в любое время (+)
# ОТВЕТ: 2,6

# 26 Интерполяция
## 1 Что проще читать -  конкатенацию или интерполяцию при длинных строках?
###	~ Интерполяцию (f-строки) (+)
###	~ Конкатенацию 
###	~ Они одинаково простые в чтении при длинных строках
###	~ Конкатенацию легко читать при длинных строка а интерполяцию удобно читать при коротких строках.
# ОТВЕТ: 1

## 2 Какие варианты кода с f-строкой выведут Hello, Arya, если name = 'Arya'?
###	~ print(f''Hello, {name}') (+)
###	~ print('f"Hello, {name}"')
###	~ print(f"Hello, {name}") (+)
###	~ print(f''Hello, name')
###	~ print('Hello, {name}')
###	~ print(Hello, name)
# ОТВЕТ: 1,3

# 27 Извлечение символов из строки.

## 1 Что будет выведено на экран?	print("Python"[0])
###	~ h
###	~ Python
###	~ n
###	~ P (+)
# ОТВЕТ: 4

## 2 Как получить последний символ строки word = 'Python'?
###	~ word[-1]  (+)
###	~ word[-2]
###	~ word[len(word)]
###	~ word[0]
###	~ word[:2]
###	~ word[-1];  (+)
# ОТВЕТ: 1,6

# 28 Срезы строк
## 1 Какой из вариантов среза перевернет строку?
### ~ [::-1] (+)
### ~ [::1]
### ~ [1:]
### ~ [ ]
# ОТВЕТ: 1

## 2 Какие срезы строки text = 'code-basics' вернут подстроку 'bas'?
###	~ print(text[5:8:1]) (+)
###	~ print(text[5:9])
###	~ print(text[5:8:2])
###	~ print(text[4:7])
###	~ print(text[5:8] (+)
###	~ print(text[8:5]
# ОТВЕТ: 1,5


# 29 Multi-line строки

## 1 Что нужно сделать, чтобы Multi-line строка не выводилась с лишней пустой строкой?
###	A)Использовать двойные кавычки ""
###	B)Добавить еще одну пустую строку перед закрывающими скобками
###	C)Не переносить закрывающие кавычки на новую строку
###	D) Писать кавычки слитно друг к другу
# Ответ: C

## 2 Какие из следующих переменных содержат многострочную строку (с фактическим символом перевода строки внутри)?
###	A) "line1\nline2" 
###	B)"""line1 
###	line2""" 
###	C)'''line1 line2'''
###	D)"line1" + "line2"
###	E) "line1\n" \ "line2"
###	F)"line1" + "n" + "line2" 
# Варианты ответов: A, B, C


# 30 Типы данных
## 1 Какой тип данных в Python предназначен для хранения дробных чисел?
###	A) int;
###	B) str
###	C) float
###	D) bool
# Ответ: C

## 2 Какие из этих вариантов при проверке вернут True?
###	A) isinstance(3.14, int)
###	B) type(10) is int
###	C) isinstance("hello", str)
###	D) isinstance(True, int)
###	E) type([1, 2]) is list
###	F) type(None) is str
# Варианты ответов: B, C, E


# 31 Сильная (или Строгая) типизация

## 1 Что из перечисленного лучше всего описывает понятие сильной типизации в языке Python?
###	A) Python автоматически преобразует типы данных, например, позволяет сложить число и строку.
###	B) Python не позволяет выполнять операции между значениями разных типов без явного приведения типов
###	C) Python проверяет соответствие типов переменных на этапе компиляции, а не во время выполнения.
###	D) Python требует явно указывать тип каждой переменной при её объявлении
# Ответ: B


## 2 ---------

# 32 Неизменяемость примитивных типов
## 1 Какие из следующих операций изменят существующий объект (а не создадут новый)?
###	A) list = [1, 2]; list.append(3)
###	B) x = 5; x += 1
###	C) s = "hello"; s.upper()
###	D) t = (1, 2); t += (3,)
# ОТВЕТ: А

## 2 Какие из следующих вариантов показывают неизменяемость примитивных типов?
###	A) x = 5
###	y = x
###	x = 10
###	print(y) 
###	B) s = "cat"
###	t = s
###	s = "dog"
###	print(t)
###	C) a = [1, 2]
###	b = a
###	a.append(3)
###	print(b)
###	D) n = 3.14
###	m = n
###	n = n + 1.0
###	print(m) 
###	E) flag = True
###	flag = False
###	F) tup = (1, 2)
###	tup[0] = 99
# Варианты ответов: A, B, D

###	
# 33 Явное преобразование типов
## 1 Почему '3' + 4 вызывает ошибку, а str(4) + '3' — нет?
#Варианты ответов:
###	1) Python приводит всё к одному типу автоматически.
###	2) В первом случае типы несовместимы, во втором — обе части строки.
###	3)Строка '3' и число 4 имеют разную длину, поэтому возникает ошибка
###	4)Оператор + работает только с числами, а str() превращает всё в число.
# ОТВЕТ: 2

## 2 Как программно проверить, что число 5 после преобразования в строку действительно стало типом str?
# Варианты ответов:
###	1)type(str(5)) is str
###	2)value = '5' print(type(5))                                                      
###	3)print(type(str))
###	4)print(type(str(5)))
###	5)isinstance(5, str)
###	6)type(str(5)) == int
# ОТВЕТ: 1,4


# 34 Функции и их вызов

## 1 Можно ли сохранять результат вызова функции в переменную?
# Варианты ответов:
###	1) Нет, функция не возвращает результат, поэтому его нельзя сохранить
###	2) Да, функция всегда возвращает результат
###	3) Нет, результат вызова функции нельзя сохранить в переменную, он сразу же выводится на экран.
###	4) Сохранять результат вызова функции в переменную можно только в очень ограниченных случаях, когда функция возвращает примитивные типы данных.
# Ответ: 2

## 2 -------------


# 35 Параметры по умолчанию
## 1 Что произойдет, если вызвать стандартную функцию pow(), не передавая в нее никаких аргументов?
# Варианты ответов:
###	1) Функция выполнится и возведет нужное число в квадрат.
###	2) Функция не выполнится и выдаст ошибку.
###	3) Функция выполнится и возведет случайное число в квадрат.
###	4) Функция pow() без аргументов вернет 1.0.
# Ответ: 2

## 2 Какие операции могут выполнять функции?
# Варианты ответов:
###	1) Выполнение арифметических вычислений и логических сравнений.
###	2) Создание новых переменных вне своей области видимости без явного указания.
###	3) Прямое управление аппаратным обеспечением компьютера.
###	4) Автоматическое исправление синтаксических ошибок в коде.
###	5) Создание новых переменных вне своей области видимости без явного указания.
###	6) Обработка данных, преобразование одних типов данных в другие.
# Ответ: 1,6


# 36 Вызов функции — выражение
## 1 Функция my_sum() принимает два числа и возвращает их сумму. Взгляните на код ниже. Какое число будет выведено на экран?
```
result = my_sum(my_sum(1, 3), my_sum(my_sum(4, 2), 3))
print(result)  # ?
```
# Варианты ответов:
### 1)  13
### 2) 10
### 3) 7
### 4) 11
# Ответ:  1)  13

## 2  Представим, что у нас есть функция square(), которая принимает число и возвращает квадрат этого числа. В каком примере мы вызываем функцию корректно и с сохранением результата в переменную result?
# Варианты ответов:
### 1) result = square(5) 
### 2) square(result = 5) 
### 3) variable_name = square(10) 
### 4) my_var = square(9)
### 5) result = square(x=7) 
### 6) square(5) = result
# ОТВЕТ: 1,4



# 37 Функции с переменным числом параметров
## 1 Можно ли комбинировать обычные параметры и вариативные в определении функции?
###	1) Можно, но вариативный параметр должен идти после обычных. +
###	2) Можно, но вариативный параметр должен идти перед обычными.
###	3) Нельзя, интерпретатор выдаст ошибку: SyntaxError: invalid syntax.
###	4) Нельзя, считается негласным правилом.
# ОТВЕТ: 1

## 2 Какие варианты кода выведут минимальное значение среди чисел 1, 2 и 3 с помощью min()?
###	1) print(min(3, 1, 2)) +
###	2) min(1,2,3) 
###	3) print(min(1, 2, 3)) +
###	4) print(min(2, 3, 1)) +
###	5) print(min(123))
###	6) MIN(3,2,1)
###	7) Min(min)
# ОТВЕТ: 1,3,4


# 38 Детерминированность
## 1 Что означает термин “Детерминированность” функции.
###	1) Функция, результат которой невозможно предугадать.
###	2) Функция, написанная неправильно.
###	3) Функция, которая всегда возвращает один и тот же результат при одинаковых входных данных. +
###	4)Функция, которая ничего не выведет на экран
# ОТВЕТ: 3


## 2 Выберите примеры детерминированных функций.
###	1) Бросок кубика с N гранями.
###	2) Функция, которая будет выводить возраст пользователя в зависимости от даты его рождения.
###	3) Повторный вывод написанного в функции слова.+
###	4) Случайное значение от 1 до 10
###	5) Функция, возвращающая длину строки. +
###	6) Функция, генерирующая рандомный пароль
###	7) Функция, которая при вводе имени человека, будет вводить его дату рождения. +
# ОТВЕТ: 3,5,7


# 39 Теория: Объекты
## 1 Метод — это……
###	1) Второе название функции без какой либо разницы в использовании.
###	2) То, что пишется в скобках после print.
###	3) Один из способов выполнения поставленной задачи.
###	4) Та же функция, но привязанная только к одному  определенному типу данных. +
# ОТВЕТ: 4

## 2 Какие утверждения о методе верны?
###	1) Отличается от функции тем, что вызывается через точку у объекта. +
###	2) Метод - это объект в сочетании с функцией.
###	3) Метод – это функции, находящиеся внутри объекта. +
###	4) Метод меняет значение переменной, на которую был вызван
###	5) Метод идет перед функцией.
###	6) Методом можно редактировать строку.+
# ОТВЕТ: 1,3,6


# 40 Неизменяемость
## 1 Какое значение содержится в переменной company?
```
company = "Hexlet"
print(company.lower())
```
###	1)hexlet 
###	2)Hexlet +
###	3)Код завершится с ошибкой
###	4)HEXLET
# ОТВЕТ: 2
## 2 Какие утверждения о неизменяемости являются верными?
###	1) Неизменяемость применяется на типы данных. +
###	2) После применения метода, безвозвратно меняется оригинал строки.
###	3) Чтобы сохранить метод - нужно создать для него переменную. +
###	4) Неизменяемость защищает от случайного изменения оригинальных данных.+
###	5) Неизменяемость относится только к типу данных bool(булевы значения).
###	6) Неизменяемость не работает при работе с str(строка).
# ОТВЕТ: 1,3,4


# 41 Цепочка вызовов
## 1 Что такое метод и цепочка методов?
### - это способ соединять несколько программ в одну большую цепочку файлов. (-)
###	- это операция, которая применяется к значению и возвращает новый результат. Если результат снова позволяет вызывать методы, то к нему можно применить еще один метод. (+)
###	- это операция, которая изменяет исходное значение, не возвращая новый результат. Цепочка методов — это когда методы записываются в случайном порядке, не завися друг от друга. (-)
###	- это последовательность функций, которые выполняются справа налево. (-)
# ОТВЕТ: 2

## 2 Что из перечисленного правда о цепочке методов?
### - Цепочка методов выполняется слева направо: каждый метод применяется к результату предыдущего. +
### - Цепочка возможна, если каждый метод в цепочке возвращает объект, у которого есть следующий вызываемый метод. +
### - В Python цепочка методов невозможна — для этого нужны специальные библиотеки.
### - В цепочке нельзя смешивать методы разных типов объектов (например, строковые и списочные).
### - Цепочка методов всегда изменяет исходный объект, а не создаёт новый.
### - Чтобы построить цепочку, после каждого метода обязательно ставить точку с запятой.
# ОТВЕТ: 1, 2 

# 42 Создание (определение) функции
## 1 Для чего функциям нужно корректно давать названия?
### - название функции должно быть коротким и запутанным, чтобы другие программисты не могли легко понять код и не украли его. (-)
### - функции помогают не только избегать дублирования, но и делают программы более понятными. Название функции само по себе говорит, что она делает. (+)
### - корректное название функции нужно для того, чтобы функция работала быстрее. (-)
### - названия функциям нужны только для того, чтобы компилятор или интерпретатор мог отличить одну функцию от другой. (-)
# ОТВЕТ: 2

## 2 Какие из следующих утверждений о функциях являются верными?
###	- функции позволяют объединить несколько составных операций в одну.(+)
###	- функции усложняют код за счет дополнительного синтаксиса, который нужно помнить. (-)
### - функции позволяют скрыть сложность какой-то составной операции. (+)
### - в Python нет возможности имплементировать свои собственные функции. (-) 
### - функции упрощают разработку и поддержку программ. (+) 
### - функции не могут возвращать результат своей работы. (-)
# ОТВЕТ: 1,3,5


# 43 Возврат значений
## 1 Почему оператор return пишется в конце функции?
### - когда python доходит до оператора return, то всё, что написано после него внутри функции, выполняться не будет. (+)
### - return нужно писать только один раз и обязательно в последней строке функции, потому что после return Python всё равно продолжает выполнять код, но делает это медленнее. (-)
### - оператор return пишется в конце функции, потому что Python требует, чтобы все функции обязательно заканчивались словом return, иначе возникнет синтаксическая ошибка. (-)
### - return пишется в конце, потому что он передает управление не туда, откуда вызвали функцию, а в самое начало программы. (-)
# ОТВЕТ: 1
## 2 Что можно сделать с возвращенным функцией значением?
### - изменить исходный код через это значение (-)
### - удалить из памяти компьютера все переменные (-)
### - сохранить в переменную (+)
### - объявить новую функцию внутри возвращенного значения (-)
### - передать в другую функцию как аргумент (+)
### - превратить его в имя переменной автоматически (-)
# ОТВЕТ: 3,5


# 44 Необязательные параметры функции

## 1 Что такое значение по умолчанию для параметра функции?
### - это значение, которое параметр получает в момент вызова функции из глобальной переменной с таким же именем. (-)
###	- это значение, которое присваивается параметру функции в её определении. (+)
### - это значение, которое Python автоматически подставляет вместо любого параметра, если в коде программы произошла ошибка. (-)
###	- это максимально возможное число, которое параметр может принять. (-)
# ОТВЕТ: 2

## 2 Что из перечисленного верно относительно необязательных параметров?
###	- Необязательные параметры должны следовать после всех обязательных параметров в определении функции. +
###	- Значение по умолчанию для необязательного параметра вычисляется только один раз — в момент определения функции, а не при каждом её вызове. +
###	- Необязательные параметры можно указывать только в конце, но при вызове их нельзя пропускать — нужно передавать None.
###	- Значением по умолчанию не может быть изменяемый объект (например, список или словарь).
###	- Если у параметра есть значение по умолчанию, то при вызове его нельзя переопределить другим значением.
###	- Необязательные параметры можно ставить перед обязательными — Python сам разберётся.
# ОТВЕТ: 1,2


# 45 Именованные аргументы
## 1 В каком порядке в определении функции должны передаваться ? 
###	А) Сначала обычные, потом именованные. +
###	Б) Сначала именованные, потом обычные. 
###	В) Сначала обязательные, потом необязательные.
###	Г) Сначала с короткими именами, потом с длинными. 
# ОТВЕТ: А

## 2 Где можно указывать аннотации типов?
###	А) my_pow(x=8, base=4) +
###	Б) my_pow(base=4, x=8)+
###	В) my_pow(4, base=8)
###	Г) my_pow(8, x=4)
###	Д) my_pow(8, 4)+
###	Е) my_pow(x=8, 4)
# ОТВЕТ: А,Б,Д


# 46 Аннотации типов
## 1 Как аннотации типов могут быть использованы внутри функции?
###	А) Для определения типов переменных.+
###	Б) Для создания новых типов данных.
###	В) Для определения порядка выполнения операций.
###	Г) Для указания имени функции.
# ОТВЕТ: А

## 2 Где можно указывать аннотации типов?
###	А) Только в комментариях.
###	Б) В определении функции, у параметров и у возвращаемого значения. +
###	В) Только в названии переменной.
###	Г) У локальных переменных внутри функции.
# ОТВЕТ: Б


# 47 Логический тип
## 1 Как называются функции, которые отвечают на вопрос «да» или «нет» и возвращают значение типа bool?
###	А) Функции-предикаты. +
###	Б) Такие функции не имеют отдельного названия.
###	В) Функции-прейскуранты.
###	Г) Утвердительные функции
# ОТВЕТ: А

## 2 Какие сравнения с использованием a = 3 и b = 5 напечатают True?
###	А) print(3 <= b) +
###	Б) print(b < a)
###	В) print(a >= b)
###	Г) print(a <= b) +
###	Д) print(a == b)
###	Е) print(a != a)
# ОТВЕТ: А,Г


# 48 Сравнение строк
## 1 Чему равна длина строки len(' 1 ')?
###	А) 0
###	Б) 1
###	В) 3 +
###	Г)2
# ОТВЕТ: В

## 2 Какие варианты проверяют, что строка "hello" начинается с подстроки "he"?
###	А) print("hello".startswith("he")) 
###	Б) print(text.startswith("he")) +
###	В) print("hello".startwith("he")) +
###	Г) print("hello".endswith("he"))
###	Д) print("hello".startswith("he", 2))
###	Е) print("hello".startswith("he") and 5 > 10)
# ОТВЕТ: Б,В


# 49 Комбинирование операций и функций

## 1 Можно ли в логических выражениях использовать результат функций? 
### а) Да, если они возвращают логический тип +
### б) Нет, функции нельзя использовать в логических выражениях
### в) Да, любые функции, независимо от возвращаемого типа, потому что любое значение автоматически приводится к логическому типу
### г) Можно, но только если функция не имеет побочных эффектов и объявлена как inline
# ОТВЕТ: a

## 2 Выберите верные утверждения:
### а) Оператор == это оператор сравнения +
### б) Оператор = это оператор присвоения  +
### в) Оператор == это оператор присвоения 
### г) Оператор = это оператор сравнения
### д) Оператор === проверяет только значение, не учитывая тип данных
### е) Оператор != означает присвоение с отрицанием
# ОТВЕТ: а, б


# 50 Логические операторы 

## 1 Каким будет результат (12 > 40) or (400 >= 400) ?
### а) False
### б) Невозможно ответить 
### в) True +
### г) Ошибка, так как нельзя сравнивать числа разных порядков без приведения типов
# ОТВЕТ: в

## 2 Как в коде обозначается оператор  и?
### а) в Python нет такого оператора 
### б) or
### в) ||
### г) and +
# ОТВЕТ: г


# 51 Отрицание 

## 1  Как работает оператор отрицания not?
### а) Всегда возвращает False
### б) Меняет логическое значение на противоположное +
### в) Отменяет действие других операторов
### г) Всегда возвращает True, потому что отрицание утверждения делает его истинным
# ОТВЕТ: б

## 2 Что подставить вместо ___, чтобы print(not ___) вывело True?
### а) 1
### б) 'text'
### в) False +
### г) 0 +
### д) True
# ОТВЕТ: в, г


# 52 Результат логических выражений 

## 1 Что будет выведено на экран? print(True or 'yes')
### а) False
### б) yes 
### г) True +
### д) Ошибка
# ОТВЕТ: г

## 2 Является ли строка '0' ложным значением в Python?
### а) Да
### б) Нет, любая непустая строка — истина +
### в) Да, потому что число 0 — ложь, и строка '0' тоже приводится к ложному значению
### г) Да, если строка '0' используется в логическом выражении, она автоматически преобразуется в число 0
# ОТВЕТ: б


# 53 Условная конструкция (if)

## 1 Почему в некоторых случаях разработчики инвертируют условие и используют not в конструкции if?
### а) Это единственный способ проверить отрицание в Python.
### б) Иногда так проще читается логика.
### в) Это требуется для работы с числами с плавающей точкой.
### г) Без not условие не может быть корректно обработано интерпретатором.
# ОТВЕТ: б

## 2 Какие условия заставят код ниже вывести yes, если x = 5? if ???: print('Yes')
###	а) x == 0
###	б) not x
###	в) x >= 1
###	г) x > 0
###	д) x < 0
# ОТВЕТ: в, г


# 54 Условная конструкция else

## 1 К какому оператору относится блок else в примере ниже?
```
if number > 0:
    print("Число положительное")
if number > 10:
    print("Число больше 10")
else:
    print("Число не положительное")
```
###	а) К первому if (number > 0)
###	б) К обоим if одновременно
###	в) Ко второму if (number > 10)
###	г) Блок else не относится ни к одному if, так как между ними нет отступа
# ОТВЕТ: в

## 2 Какие условия без отрицания проверит, что последний символ строки — вопросительный знак?
```
last_char = sentence[-1]
if ???:
    print('question')
else:
    print('normal')
```
###	а) last_char is ‘?’
###	б) ‘?’ == last_char
###	в) ‘?’ in last_char
###	г) last_char != ‘?’
###	д) last_char == ‘?’
# ОТВЕТ: б, д


# 55 Конструкция else + if= elif

## 1 Можно ли иметь несколько блоков elif в одной конструкции?
###	а) Только одну
###	б) Сколько нужно (любое количество)
###	в) Синтаксис Python запрещает несколько elif
###	г) При условии, что все условия проверяют одну переменную
# ОТВЕТ: б

## 2 В Python конструкция elif является сокращением для какой комбинации ключевых слов?
###	а) else if
###	б) else + if (последовательное использование)
###	в) else and if
###	г) else uness
###	д) else then if
# ОТВЕТ: а, б


# 56 Тернарный оператор

## 1 Рассмотрите код:
```
num = 6
result = num + 1 if num % 2 != 0 else 1 - num
print(result)
```
## Что выведет код?
###	а) 7
###	б) -6
###	в) 1
###	г) 0
###	д) -5 
# ОТВЕТ: д

## 2 Какие выражения с тернарным оператором возвращают модуль числа n?
###	а) result = n if n >= 0 else -n
###	б) result = n if n = 0 else n
###	в) result = -n if n < 0 else -n
###	г) result = n if n =< 0 else -n
###	д) result = -n if n =< 0 else n
###	е) result = -n if n < 0 else n
# ОТВЕТ: а, е

# 57 Оператор Match
## 1 Какое ключевое слово в конструкции match используется для обозначения ситуации, которая соответствует ветке else в условных конструкциях if-else?
# Варианты ответов:
### 1.else
### 2.other
### 3.case 
### 4.default
# Ответ: 3

# 2 Дан код обработки статуса заказа.
Выберите все значения переменной order_status, при которых на экран выведется сообщение “Действие: Отправка уведомления”:
```
order_status = . . . #Некоторые значения
match order_status: ???
	case “new”:
		print (“Действие: Создание черновика”)
	case “paid” | “processing”:
		print (“Действие: Отправка уведомления”)
	case “sheeped”:
		print (“Действие: Передача в доставку”)
	case _:
		print(“Действие: Неизвестный статус”)
```
# Варианты ответов:
###	1.”new”
###	2.”paid”
###	3.”processing”
###	4.”shipped”
###	5.”delivered”
###	6.”PAID”
# Ответ: 2,3


# 58 Цикл While
## 1 Что произойдет, если в цикле while не изменять переменную-счётчик, которая участвует в условии продолжения?
# Варианты ответов:
###	1.Цикл выполнится ровно один раз
###	2.Возникает синтаксическая ошибка
###	3.Цикл станет бесконечным
###	4.Цикл не выполнится ни разу
# Правильный ответ: 3

## 2 Дан код для подсчёта суммы квадратов чисел от 1 до некоторого предела. Выберите все верные утверждения:
```
def sum_of_squares(limit: int) -> int:
    current = 1
    total = 0
    while current <= limit:
        total = total + current * current
        current = current + 1
    return total
result = sum_of_squares(3)
print(result)
```
# Варианты ответов:
###	1.Функция вернёт значение 14 
###	2.Переменная total на первой итерации станет равна 1
###	3.Если вызвать sum_of_squares(0), функция вернёт 0
###	4.Строка current = current + 1 выполняется до вычисления квадрата
###	5.Если заменить while current <= limit: на while current < limit:, результат для limit=3 не изменится
###	6.Переменная limit изменяется внутри цикла
# Правильные ответы: 1, 2, 3


# 59 Агрегация данных (Числа)
## 1 В задачах на агрегацию данных всегда присутствует особая переменная. Какую роль она выполняет?
# Варианты ответов:
###	1.Хранит количество выполненных итераций
###	2.Хранит внутри себя накапливаемый результат работы цикла
###	3.Хранит исходные данные до начала обработки
###	4.Определяет максимальное количество итераций
# Правильныe ответ: 2

## 2 Дан код для поиска минимального значения в списке. Выберите все верные утверждения:
```
def find_minimum(numbers: list[int]) -> int | None:
    if len(numbers) == 0:
        return None
    min_value = numbers[0]
    index = 1
    while index < len(numbers):
        if numbers[index] < min_value:
            min_value = numbers[index]
        index = index + 1
        return min_value
data = [8, 3, 12, 5, 3, 9]
print(find_minimum(data))
```
# Варианты ответов:
###	1.Функция вернёт значение 3
###	2.Нейтральным элементом для поиска минимума здесь выступает первый элемент списка numbers[0]
###	3.Переменная index начинает работу со значения 0
###	4.Если передать пустой список, функция вернёт None
###	5.Если в списке несколько одинаковых минимальных значений, функция вернёт только первое из них
###	6.Цикл выполнит ровно len(numbers) итераций
# Правильные ответы: 1, 2, 4, 5


# 60 Агрегация данных (Строки)

## 1 В разделе «Агрегация данных (Строки)» рассматривается функция repeat, которая «наращивает» строку в цикле. Без какого элемента механизм наращивания не сможет корректно стартовать и вернуть правильный результат?
# Варианты ответов:
###	1.Без счётчика цикла i
###	2.Без входного аргумента times
###	3.Без нейтрального элемента — пустой строки '' ''
###	4.Без оператора return
# Правильный ответ: 3

## 2 Дан код для формирования строки из элементов списка с разделением.
```
def join_with_dash(items: list[str]) -> str:
    result = ""
    position = 0
    while position < len(items):
        result = result + items[position]
        if position != len(items) - 1:
            result = result + "-"
        position = position + 1
    return result
words = ["apple", "banana", "cherry"]
print(join_with_dash(words))
```
# Варианты ответов:
###	1.Функция вернёт строку "apple-banana-cherry"
###	2.На первой итерации цикла к result добавится "apple-"
###	3.Если список items пустой, функция вернёт пустую строку ""
###	4.Проверка if position != len(items) - 1 нужна, чтобы не ставить дефис после последнего элемента
###	5.Переменная result на последней итерации будет содержать значение "apple-banana-"
###	6.Нейтральный элемент "" позволяет корректно начать накопление с первого слова
# Правильные ответы: 1, 3, 4, 6


# 61 --------

## 1
###	
## 2
###	


# 62 ------
## 1

## 2
###	


# 63 ----------
## 1

## 2


# 64 --------
## 1

## 2


# 65 Цикл FOR

## 1 Что произойдет при выполнении этого кода?
```
s = ‘123’
for symbol in s:
	print(int(symbol) * 2)
```
###	A) Выведется 246
###	B) Выведется 2, затем 4, затем 6 (каждое на новой строке) +
###	C) Ошибка, потому что символ нельзя преобразовать в число 
###	D) Выведется 123123
# ОТВЕТ: B

## 2 Какие два утверждения о цикле for в Python являются верным?
###	A) Код, использующий цикл for, нельзя переписать с помощью цикла while
###	B) При итерировании по строке в переменную цикла на каждой итерации попадает текущий символ строки +
###	C) Цикл for всегда требует явного создания счётчика
###	D) Код for ch in text: является правильной записью для перебора символов строки text +
###	E) Цикл for не может работать со строками
###	F) При итерировании по строке в переменную цикла попадает индекс текущего символа
# ОТВЕТ: В, D


# 66 Цикл for и функция range 

## 1 Какой тип последовательности создает функция range()
###	A) Последовательность букв
###	B) Последовательность чисел внутри определенного диапазона +
###	C) Последовательность  символов
###	D) Последовательность логических значений 
# ОТВЕТ: B

## 2 Какие утверждения о работе цикла for с функцией range() соответствуют действительности?
###	A) range(5) сгенерирует числа 1, 2, 3, 4, 5
###	B) Если написать for i in range(10):, то цикл выполнится 10 раз +
###	C) range(2, 5) сгенерирует числа 2, 3, 4, 5
###	D) В range(start, stop, step) параметр step может быть отрицательным +
###	E) Цикл for i in range(10, 5): выведет числа от 10 до 5
###	F) Функцию range() нельзя использовать без аргументов
# ОТВЕТ: B, D


# 67 Отладка
## 1 Какая общая идея объединяет все способы отладки программ?
###	A) Ускорение работы программы
###	B) Анализ того, как меняются значения переменных в процессе работы кода + 
###	C) Уменьшение размера кода
###	D) Автоматическое исправление ошибок
# ОТВЕТ: B

## 2 Перед отладкой кода надо понять, что в нем не так. Это можно сделать за два шага.
###	A) Шаг 1 Запустить код на другом компьютере
###	B) Шаг 1 Изучить traceback - список всех вызовов функций от запуска программы до места ошибки +
###	C) Шаг 1 Сразу удалить половину кода и написать заново
###	D) Шаг 2 Использовать метод «случайной правки»
###	E) Шаг 2 Прочитать сообщение об ошибке (например, NameError: name 'create' is not defined) +
###	F) Шаг 2 Попросить коллегу запустить код, ничего не объясняя
# ОТВЕТ: B, E

# 68 Модули
## 1 В файле main.ру есть строка import utils. Как правильно вызвать функцию make_stuff() из модуля utils?
###	A) make_stuff()
###	B) utils.make_stuff() +
###	C) import utils.make_stuff()
###	D) utils -> make_stuff()
# ОТВЕТ: B

## 2 Какие три способа импорта модулей в Python?
###	A) Импорт только документации модуля
###	B) Импорт модуля целиком +
###	C) Импорт модуля с автоматическим выполнением 
###	D) Импорт отдельных определений из модуля  +
###	E) Импорт модуля порциями с автоматической загрузкой недостающих атрибутов
###	F) Импорт всего содержимого модуля сразу +
# ОТВЕТ: B, D, F


# 69 Модули поглубже -----
## 1
###	
## 2


# 70 Пакеты -----
## 1
###	
## 2


# 71 Модуль random  ------
## 1
###	
## 2


# 72 Кортежи -----
## 1

## 2


# 70 Пакеты  -----
## 1

## 2
###	


# 71 Модуль random -------
## 1
###	
## 2
###	


# 72 Кортежи -----
## 1

## 2

 
# 73 История развития языка Python -------
## 1
###	
## 2
